[pytest]
addopts = -s --tb=short -m "not memory"
norecursedirs = __pycache__ build
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    memory: slow opt-in tests that measure peak RSS to calibrate the estimate_memory formula (run via `pytest -m memory`)
