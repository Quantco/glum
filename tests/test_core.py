def test_hard():
    import glm_benchmarks  # noqa

    # this is just a demo that pytest can produce good error messages just by parsing assert statements
    assert dict(a=1, b=2) == dict(a=1, b=2)
